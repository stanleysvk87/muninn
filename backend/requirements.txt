# Pinned deliberately. These used to be 11 bare names with no version and no
# lockfile, so two `docker compose build` runs a week apart could resolve
# different versions -- a major bump of e.g. `anthropic` would silently break
# extraction with no change to our own code. The versions below are what the
# working backend venv resolves as of 2026-07-28; bump them consciously (and
# re-run the test suite) instead of letting a rebuild do it silently.
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic-settings==2.14.2
cryptography==50.0.0
anthropic==0.121.0
watchdog==6.0.0
python-multipart==0.0.32
Pillow==12.3.0
httpx==0.28.1
pypdf==6.15.0
PyMuPDF==1.28.0
